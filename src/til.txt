============================================================
TIL - TODAY I LEARNED
Auteur      : Yann Ngateu
Période     : Janvier 2026 - Février 2026
Objectif    : Capitaliser les problèmes techniques rencontrés
============================================================

============================================================
TIL-001 — Docker Compose sans Dockerfile
============================================================
Date        : 2026-01-07
Projet      : Smart Valuator / Phoenix
Catégorie   : Docker

PROBLEME
Erreur lors d’un docker compose build sans Dockerfile.

CAUSE
Les images officielles sont déjà prêtes.
Aucun Dockerfile n’est nécessaire.

SOLUTION
docker compose up -d

LECON
Docker Compose peut fonctionner sans Dockerfile.


============================================================
TIL-002 — Erreur 500 Spring Boot à cause de réponse IA mal formée
============================================================
Date        : 2026-01-09
Projet      : Smart Valuator API
Catégorie   : Spring Boot / IA

PROBLEME
Erreur 500 lors du parsing d’une réponse IA.

CAUSE
La réponse contenait un JSON encapsulé dans des balises markdown.

SOLUTION
Nettoyer la réponse avant parsing
ou imposer un format strict côté prompt.

LECON
Une API ne doit jamais faire confiance à une source externe.


============================================================
TIL-003 — Commit Git mal nommé
============================================================
Date        : 2026-01-10
Projet      : Tous
Catégorie   : Git

PROBLEME
Historique Git peu lisible.

CAUSE
Messages de commit vagues ou incohérents.

SOLUTION
Utiliser une convention :
feat, fix, refactor, chore, test.

LECON
Un bon commit explique le pourquoi, pas seulement le quoi.


============================================================
TIL-004 — Push Git refusé (non-fast-forward)
============================================================
Date        : 2026-01-11
Projet      : Tous
Catégorie   : Git / GitHub

PROBLEME
git push refusé avec non-fast-forward.

CAUSE
La branche distante contient des commits absents en local.

SOLUTION
git pull --rebase
ou
git pull puis résoudre les conflits.

LECON
Toujours synchroniser avant de pousser.


============================================================
TIL-005 — Git pull déclenche un conflit
============================================================
Date        : 2026-01-13
Projet      : Tous
Catégorie   : Git

PROBLEME
Conflit lors d’un git pull.

CAUSE
Deux modifications concurrentes sur les mêmes lignes.

SOLUTION
Résoudre manuellement les conflits
puis :
git add .
git commit

LECON
Les conflits sont normaux dans un projet vivant.


============================================================
TIL-006 — Mauvaise branche utilisée pour un commit
============================================================
Date        : 2026-01-15
Projet      : Tous
Catégorie   : Git

PROBLEME
Commit réalisé sur la mauvaise branche.

CAUSE
Absence de vérification avant commit.

SOLUTION
git checkout bonne-branche
git cherry-pick <commit>

LECON
Toujours vérifier sa branche avant de coder.


============================================================
TIL-007 — Authentification GitHub refusée
============================================================
Date        : 2026-01-16
Projet      : Tous
Catégorie   : GitHub / Sécurité

PROBLEME
GitHub refuse le mot de passe.

CAUSE
Suppression de l’authentification par mot de passe.

SOLUTION
Utiliser un Personal Access Token
ou configurer une clé SSH.

LECON
Les outils évoluent, la sécurité aussi.


============================================================
TIL-008 — Problème CORS entre Angular et Spring Boot
============================================================
Date        : 2026-01-18
Projet      : Smart Valuator
Catégorie   : Web / Sécurité

PROBLEME
Requêtes bloquées par le navigateur.

CAUSE
Origines différentes non autorisées.

SOLUTION
Configurer CORS côté backend.

LECON
Le navigateur applique une sécurité stricte par défaut.


============================================================
TIL-009 — Angular bloqué sur un loader infini
============================================================
Date        : 2026-01-20
Projet      : Smart Valuator Frontend
Catégorie   : Angular / HTTP

PROBLEME
Chargement infini de l’interface.

CAUSE
Observable non terminé ou erreur non gérée.

SOLUTION
Toujours utiliser catchError et finalize.

LECON
Un loader doit toujours avoir une sortie.


============================================================
TIL-010 — Git ignore les fichiers modifiés
============================================================
Date        : 2026-01-22
Projet      : Tous
Catégorie   : Git

PROBLEME
git status n’affiche pas certains fichiers.

CAUSE
Fichiers présents dans .gitignore.

SOLUTION
Vérifier .gitignore
ou forcer avec :
git add -f

LECON
.gitignore est plus puissant qu’on ne le pense.


============================================================
TIL-011 — Git reset mal utilisé
============================================================
Date        : 2026-01-24
Projet      : Tous
Catégorie   : Git

PROBLEME
Perte apparente de commits.

CAUSE
Utilisation de git reset --hard.

SOLUTION
Utiliser :
git reflog
pour récupérer les commits.

LECON
Git garde presque toujours une trace.


============================================================
TIL-012 — pgAdmin redémarre en boucle
============================================================
Date        : 2026-01-28
Projet      : Phoenix Exchange
Catégorie   : Docker / PostgreSQL

PROBLEME
pgAdmin reste en restarting.

CAUSE
Email invalide dans la configuration.

SOLUTION
Utiliser un email valide
et supprimer les volumes Docker.

LECON
Une configuration invalide peut bloquer tout un service.


============================================================
TIL-013 — Connexion PostgreSQL impossible depuis pgAdmin
============================================================
Date        : 2026-02-01
Projet      : Phoenix Exchange
Catégorie   : Docker / Réseau

PROBLEME
Connexion refusée avec localhost.

CAUSE
localhost pointe vers le conteneur.

SOLUTION
Utiliser le nom du service Docker.

LECON
Docker fournit son propre réseau interne.


============================================================
TIL-014 — Scripts SQL ignorés par PostgreSQL
============================================================
Date        : 2026-02-04
Projet      : Phoenix Exchange
Catégorie   : Docker / PostgreSQL

PROBLEME
Les scripts SQL ne s’exécutent pas.

CAUSE
Ils sont lancés uniquement au premier démarrage.

SOLUTION
Supprimer le volume PostgreSQL.

LECON
L’initialisation de base est one-shot.


============================================================
TIL-015 — JWT invalide ou expiré
============================================================
Date        : 2026-02-07
Projet      : Phoenix Exchange
Catégorie   : Sécurité / Auth

PROBLEME
Toutes les requêtes retournent 403.

CAUSE
Token expiré ou mal injecté.

SOLUTION
Vérifier le header Authorization
et utiliser un interceptor.

LECON
Les erreurs 403 cachent souvent un problème de token.


============================================================
TIL-016 — Docker : No space left on device
============================================================
Date        : 2026-02-11
Projet      : Système
Catégorie   : DevOps

PROBLEME
Impossible de build ou lancer un conteneur.

CAUSE
Accumulation d’images et volumes inutilisés.

SOLUTION
docker system prune -a --volumes

LECON
Docker doit être nettoyé régulièrement.

============================================================
TIL-017 — Échec de la dérivation de requête Spring Data JPA
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Data JPA

PROBLEME
L'application crash au démarrage avec l'erreur : No property 'filters' found for type 'Product'.

CAUSE
Spring tente de générer automatiquement une requête SQL à partir du nom de la méthode findByFilters. Comme l'attribut filters n'existe pas dans l'entité Product, le parsing échoue.

SOLUTION
Utiliser l'annotation @Query pour définir manuellement le JPQL ou renommer la méthode selon les attributs réels de l'entité.

LECON
La "Query Derivation" est pratique mais limitée ; pour des filtres complexes, le JPQL explicite est préférable.

============================================================
TIL-018 — Conflit d'import entre AWT et Spring Data
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Java / Spring Boot

PROBLEME
Erreur de type : Required type: java.awt.print.Pageable, Provided: org.springframework.data.domain.Pageable.

CAUSE
L'auto-import d'IntelliJ a sélectionné la classe Pageable du JDK (destinée à l'impression physique) au lieu de celle de Spring Data.

SOLUTION
Remplacer import java.awt.print.Pageable; par import org.springframework.data.domain.Pageable;.

LECON
Toujours vérifier le package des classes suggérées par l'IDE, surtout pour les noms génériques.

============================================================
TIL-019 — Erreur de casting dans la config de Sécurité
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Security

PROBLEME
ClassCastException: BCryptPasswordEncoder cannot be cast to UserDetailsService.

CAUSE
Dans SecurityConfig, le bean PasswordEncoder a été passé par erreur au constructeur du DaoAuthenticationProvider là où le UserDetailsService était attendu.

SOLUTION
Vérifier l'ordre des paramètres et utiliser les setters/constructeurs de manière explicite avec les bons types injectés.

LECON
La configuration manuelle des Beans de sécurité demande une vigilance accrue sur l'injection des dépendances.

============================================================
TIL-020 — Placeholder non résolu (Variables d'env)
============================================================
Date        : 2026-02-26
Projet      : Smart Valuator API
Catégorie   : Configuration / Spring Boot

PROBLEME
Could not resolve placeholder 'HF_API_KEY'.

CAUSE
L'application utilise @Value("${HF_API_KEY}") mais la variable n'est définie ni dans application.properties, ni dans l'environnement du système ou de l'IDE.

SOLUTION
Ajouter la variable dans les "Environment Variables" de la configuration de lancement d'IntelliJ ou la passer en argument Maven : -Dspring-boot.run.arguments="--HF_API_KEY=...".

LECON
Spring Boot échoue "fail-fast" si une propriété requise est manquante au démarrage.

============================================================
TIL-021 — Breaking Change Jackson 3 (Spring Boot 4)
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Jackson / JSON

PROBLEME
No enum constant tools.jackson.databind.SerializationFeature.write-dates-as-timestamps.

CAUSE
Migration vers Jackson 3.0. Cette option de sérialisation a été renommée ou supprimée de l'énumération car le format ISO-8601 est devenu le standard natif.

SOLUTION
Supprimer la ligne spring.jackson.serialization.write-dates-as-timestamps du fichier properties.

LECON
Les montées de version majeure (Spring Boot 3 vers 4) invalident souvent d'anciennes configurations de sérialisation.

============================================================
TIL-022 — Conflit de lancement Spring Batch multi-jobs
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Batch

PROBLEME
Job name must be specified in case of multiple jobs.

CAUSE
Spring Batch tente de lancer tous les Jobs détectés au démarrage. S'il y en a plusieurs, il ne sait pas lequel choisir.

SOLUTION
Désactiver le lancement automatique via spring.batch.job.enabled=false et laisser le Scheduler gérer les exécutions.

LECON
Dans une architecture Web, il est souvent préférable de désactiver l'exécution automatique des Batchs au démarrage du serveur.

============================================================
TIL-023 — Version JJWT incompatible avec Spring Boot 4
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Dépendances / JWT

PROBLEME
Méthodes JWT obsolètes ou comportement inattendu avec JJWT 0.11.5.

CAUSE
JJWT 0.11.5 est une version ancienne. Spring Boot 4 recommande 0.12.x minimum.

SOLUTION
Mettre à jour dans pom.xml :
```xml
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.5</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.5</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.5</version>
    <scope>runtime</scope>
</dependency>
```

Et adapter le code JwtTokenProvider pour utiliser la nouvelle API.

LECON
Les versions milestone de Spring Boot nécessitent des dépendances à jour.

============================================================
TIL-024 — Query Derivation échoue avec méthode complexe
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Data JPA

PROBLEME
No property 'filters' found for type 'Product' lors du démarrage Spring Boot.

CAUSE
La méthode findByFilters(category, brand, status, pageable) ne suit pas la convention de nommage Spring Data. Spring tente de chercher un attribut "filters" inexistant.

SOLUTION
Option 1 : Utiliser @Query avec JPQL
```java
@Query("SELECT p FROM Product p WHERE " +
       "(:category IS NULL OR p.category = :category) AND " +
       "(:brand IS NULL OR p.brand = :brand) AND " +
       "(:status IS NULL OR p.status = :status)")
Page<Product> findByFilters(@Param("category") String category,
                            @Param("brand") String brand,
                            @Param("status") ProductStatus status,
                            Pageable pageable);
```

Option 2 : Renommer selon convention
```java
Page<Product> findByCategoryAndBrandAndStatus(
    String category, String brand, ProductStatus status, Pageable pageable);
```

LECON
Les méthodes de repository doivent soit suivre la convention de nommage stricte, soit utiliser @Query.

============================================================
TIL-025 — spring-boot-starter-webmvc n'existe pas
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Maven / Dépendances

PROBLEME
Could not find artifact org.springframework.boot:spring-boot-starter-webmvc.

CAUSE
Le nom correct est spring-boot-starter-web, pas webmvc.

SOLUTION
Remplacer dans pom.xml :
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-webmvc</artifactId>
</dependency>
```
Par :
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>
```

LECON
Les starters Spring Boot ont des noms précis. En cas de doute, consulter la doc officielle.

============================================================
TIL-026 — Dépendances de test invalides dans Spring Boot 4
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Maven / Tests

PROBLEME
Artifacts non trouvés : spring-boot-starter-*-test avec suffixes spécifiques.

CAUSE
Spring Boot 4 n'a pas de starters de test individuels. Tous les tests sont dans spring-boot-starter-test.

SOLUTION
Supprimer :
- spring-boot-starter-data-jpa-test
- spring-boot-starter-flyway-test
- spring-boot-starter-validation-test
- spring-boot-starter-webmvc-test

Garder uniquement :
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security-test</artifactId>
    <scope>test</scope>
</dependency>
<dependency>
    <groupId>org.springframework.batch</groupId>
    <artifactId>spring-batch-test</artifactId>
    <scope>test</scope>
</dependency>
```

LECON
Spring Boot 4 simplifie les dépendances de test en les regroupant.

============================================================
TIL-027 — springdoc-openapi version incompatible
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Swagger / Documentation

PROBLEME
springdoc-openapi-starter-webmvc-ui 2.0.2 ne fonctionne pas avec Spring Boot 4.

CAUSE
Spring Boot 4 nécessite springdoc-openapi 2.3.x minimum.

SOLUTION
Mettre à jour dans pom.xml :
```xml
<dependency>
    <groupId>org.springdoc</groupId>
    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    <version>2.3.0</version>
</dependency>
```

LECON
Les versions milestone de Spring Boot cassent souvent la compatibilité avec les librairies tierces.

============================================================
TIL-028 — Flyway et Spring Boot 4 incompatibilité
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Flyway / Migration

PROBLEME
Flyway ne démarre pas ou génère des erreurs de baseline avec Spring Boot 4.0.2.

CAUSE
Spring Boot 4 utilise Flyway 10.x qui a des breaking changes.

SOLUTION
Option 1 : Downgrade Spring Boot vers 3.3.x (version stable)
```xml
<version>3.3.0</version>
```

Option 2 : Mettre à jour les scripts Flyway et configuration
```properties
spring.flyway.baseline-version=0
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false
```

LECON
Spring Boot 4.0.x est en milestone/snapshot. Pour un projet en production, utiliser 3.3.x (version LTS).

============================================================
TIL-029 — ClassCastException BCryptPasswordEncoder vs UserDetailsService
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Security

PROBLEME
java.lang.ClassCastException: BCryptPasswordEncoder cannot be cast to UserDetailsService.

CAUSE
Erreur d'injection dans DaoAuthenticationProvider. Le PasswordEncoder a été passé là où le UserDetailsService était attendu.

SOLUTION
Vérifier l'ordre des paramètres et utiliser les setters explicitement :
```java
@Bean
public DaoAuthenticationProvider authenticationProvider() {
    DaoAuthenticationProvider provider = new DaoAuthenticationProvider();
    provider.setUserDetailsService(userDetailsService);
    provider.setPasswordEncoder(passwordEncoder());
    return provider;
}
```

LECON
Les beans Spring doivent être injectés dans le bon ordre avec les bons types.

============================================================
TIL-030 — Lombok @Data ne génère pas les getters/setters
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Lombok / Annotation Processing

PROBLEME
Getters/setters absents malgré @Data sur les entités/DTOs.

CAUSE
Annotation processing non configuré dans maven-compiler-plugin.

SOLUTION
Ajouter dans pom.xml :
```xml
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-compiler-plugin</artifactId>
    <configuration>
        <annotationProcessorPaths>
            <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </path>
        </annotationProcessorPaths>
    </configuration>
</plugin>
```

Et activer Annotation Processing dans IntelliJ :
Settings → Build → Compiler → Annotation Processors → Enable annotation processing

LECON
Lombok nécessite une configuration explicite du traitement des annotations.

============================================================
TIL-031 — Repository méthode retourne Optional mais service attend Entity
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Data JPA

PROBLEME
NullPointerException ou Optional.empty() non géré.

CAUSE
Les méthodes findById retournent Optional<Entity> mais le code appelle directement .get() sans vérification.

SOLUTION
Toujours gérer Optional :
```java
//Mauvais
Product product = productRepository.findById(id).get();

//Bon
Product product = productRepository.findById(id)
    .orElseThrow(() -> new ResourceNotFoundException("Product not found: " + id));
```

LECON
Optional doit toujours être déballé avec orElse, orElseThrow ou orElseGet.

============================================================
TIL-032 — @Transactional manquant sur les services
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : JPA / Transactions

PROBLEME
LazyInitializationException : could not initialize proxy - no Session.

CAUSE
Relations lazy-loaded accédées hors transaction.

SOLUTION
Ajouter @Transactional sur les classes de service :
```java
@Service
@Transactional
@RequiredArgsConstructor
public class ProductService {
    // ...
}
```

LECON
Les services qui manipulent des entités JPA doivent être transactionnels.

============================================================
TIL-033 — @PreAuthorize ne fonctionne pas
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Security

PROBLEME
Les restrictions @PreAuthorize sont ignorées, tous les endpoints sont accessibles.

CAUSE
@EnableMethodSecurity non ajouté dans SecurityConfig.

SOLUTION
Ajouter l'annotation :
```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity  // ← Important !
public class SecurityConfig {
    // ...
}
```

LECON
La sécurité au niveau méthode nécessite une activation explicite.

============================================================
TIL-034 — Port 8080 déjà utilisé
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Spring Boot / Configuration

PROBLEME
Web server failed to start. Port 8080 was already in use.

CAUSE
Une autre application (Tomcat, autre Spring Boot, etc.) utilise déjà le port.

SOLUTION
Option 1 : Changer le port dans application.properties
```properties
server.port=8081
```

Option 2 : Tuer le processus :
Windows : `netstat -ano | findstr :8080` puis `taskkill /PID <pid> /F`
Linux : `lsof -i :8080` puis `kill -9 <pid>`

LECON
Toujours vérifier les ports utilisés avant de lancer une application.

============================================================
TIL-035 — JwtTokenProvider utilise API obsolète
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : JWT / JJWT

PROBLEME
Méthodes setSigningKey, parseClaimsJws obsolètes dans JJWT 0.12.x.

CAUSE
API JJWT refactorisée entre 0.11 et 0.12.

SOLUTION
Mettre à jour le code :
```java
// Ancien (0.11.x)
Jwts.builder()
    .setSubject(username)
    .signWith(SignatureAlgorithm.HS512, secret);

// Nouveau (0.12.x)
SecretKey key = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
Jwts.builder()
    .subject(username)
    .signWith(key);
```

LECON
Toujours consulter le changelog lors d'une mise à jour de version majeure.

============================================================
TIL-036 — PostgreSQL rejette les migrations Flyway
============================================================
Date        : 2026-02-26
Projet      : Phoenix Exchange API
Catégorie   : Flyway / PostgreSQL

PROBLEME
FlywayException: Validate failed: Migration checksum mismatch.

CAUSE
Un script Flyway a été modifié après avoir été exécuté.

SOLUTION
Option 1 (DEV uniquement) : Supprimer la table flyway_schema_history
```sql
DROP TABLE flyway_schema_history;
```

Option 2 (PROD) : Créer une nouvelle migration corrective
```sql
-- V3__fix_previous_migration.sql
```

LECON
Ne JAMAIS modifier un script Flyway déjà exécuté en production.

============================================================
TIL-037 — Le dossier .idea ne veut pas être ignoré
============================================================
Date        : 2026-02-26
Projet      : RememberthDay
Catégorie   : Android App

PROBLÈME
Le dossier .idea ne veut pas être ignoré malgré son ajout dans le fichier .gitignore.

CAUSE
Le dossier .idea a été ajouté au dépôt avant son ajout dans le .gitignore.

SOLUTION
Supprimer le dossier du suivi Git (git rm -r --cached .idea), puis commit et push à nouveau.
Le dossier sera recréé automatiquement par l’IDE, et le .gitignore fonctionnera correctement.

LEÇON
Toujours configurer correctement le .gitignore avant le premier commit.

============================================================
TIL-038 — Accès Base de données sur le Main Thread
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android / Room

PROBLEME
L'application crash immédiatement au lancement ou lors d'une action avec l'erreur :
"Cannot access database on the main thread".

CAUSE
Room interdit par défaut les opérations d'E/S (Input/Output) sur le thread principal pour éviter de figer l'interface utilisateur.

SOLUTION
Utiliser un exécuteur (`AppDatabase.databaseWriteExecutor.execute(...)`) pour effectuer les opérations DAO en arrière-plan.

LECON
Toute interaction avec SQLite doit être asynchrone pour garantir la fluidité de l'UI.


============================================================
TIL-039 — ClassCastException entre Button et EditText
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android / Java

PROBLEME
Crash de l'application : "AppCompatButton cannot be cast to android.widget.EditText".

CAUSE
Une variable déclarée comme EditText dans le code Java a été liée via findViewById à un bouton dans le layout XML.

SOLUTION
Harmoniser les types : si l'ID pointe vers un bouton, la variable Java doit être de type Button ou View (type générique).

LECON
La correspondance entre les IDs du XML et les types de variables Java doit être rigoureuse.


============================================================
TIL-040 — Perte de données au clic (Race Condition)
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android / Threading

PROBLEME
Le nom du contact disparaît ou les données se mélangent lorsqu'on passe d'un contact à un autre dans la liste.

CAUSE
Une sauvegarde automatique asynchrone tentait de lire le contenu des champs texte au moment précis où l'UI les vidait pour afficher le contact suivant.

SOLUTION
Capturer les valeurs des champs texte dans des variables locales finales AVANT de lancer le thread de sauvegarde.

LECON
L'état des vues UI est instable ; il faut figer les données avant de les traiter en arrière-plan.


============================================================
TIL-041 — Échec de mise à jour UI depuis un thread de fond
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android / UI

PROBLEME
Les modifications (suppression, chargement) ne sont pas visibles à l'écran ou provoquent un crash intermittent.

CAUSE
Tentative de modifier des composants de l'interface (Adapter, Toasts, Visibility) depuis un thread secondaire.

SOLUTION
Encapsuler les appels de mise à jour visuelle dans la méthode runOnUiThread(() -> { ... }).

LECON
Seul le thread principal est autorisé à toucher à l'interface graphique.


============================================================
TIL-042 — NotificationService : Toast Queue Full
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android / OS

PROBLEME
Logcat : "Package has already queued 5 toasts. Not showing more."

CAUSE
L'application envoie trop de messages Toast à la suite (boucles ou clics rapides), saturant la file d'attente du système Android.

SOLUTION
Limiter les messages de feedback ou utiliser des Snackbars qui remplacent la notification précédente au lieu de s'empiler.

LECON
Un feedback utilisateur excessif peut être perçu comme du spam par le système d'exploitation.


============================================================
TIL-043 — Database Inspector : Données non synchronisées
============================================================
Date        : 2026-03-06
Projet      : RememberthDay
Catégorie   : Android Studio / Debug

PROBLEME
Les données de test (Mock Data) n'apparaissent pas malgré les modifications du code.

CAUSE
L'application utilise le fichier de base de données déjà créé lors du premier lancement, ignorant les nouvelles conditions d'insertion.

SOLUTION
Désinstaller l'application du terminal pour supprimer physiquement le fichier .db et forcer une nouvelle initialisation.

LECON
Le cycle de vie de la base de données Room persiste au-delà du simple redémarrage de l'application.

============================================================
TIL-044 — ContactAdapter : Séparation du modèle de données
============================================================
Date        : 2026-03-08
Projet      : RememberthDay
Catégorie   : Android / Architecture

PROBLEME
Le ContactAdapter héritait de RecyclerView.Adapter et gérait
manuellement la liste via setContacts() + notifyDataSetChanged(),
forçant un re-rendu complet à chaque mise à jour.

CAUSE
Sans DiffUtil, l'adapter ne peut pas calculer les différences
entre l'ancienne et la nouvelle liste. Il doit tout recalculer,
ce qui supprime les animations et dégrade les performances.

SOLUTION
Migrer vers ListAdapter<Contact, ViewHolder> qui intègre
DiffUtil nativement via un ItemCallback :
- areItemsTheSame()    → compare les IDs (même entité ?)
- areContentsTheSame() → compare les champs (re-rendu utile ?)
Remplacer setContacts(list) par submitList(list), qui est
asynchrone et thread-safe.

LECON
ListAdapter délègue la gestion de la liste et du diff au
framework. L'adapter ne contient plus qu'une seule
responsabilité : lier les données à la vue (onBindViewHolder).

============================================================
TIL-045 — Prepa CCST : Module 1, la communication dans un monde connecte
============================================================
Date        : 2026-07-14
Projet      : Prepa CCST Networking
Catégorie   : Module 1

AVANCEMENT
100% - module termine.

ETAT
Module boucle. Je maitrise les bases de la communication reseau et le vocabulaire de depart.

NOTIONS VUES
- Types de reseaux selon la taille : domestique, petite entreprise (SOHO), moyen/grand, mondial (Internet).
- Difference bande passante (capacite max theorique) et debit (ce qui passe reellement).
- Roles client / serveur / pair a pair : un appareil peut jouer les deux.

A RETENIR
Le classement des reseaux se fait par la taille et le nombre d'utilisateurs, pas par le materiel.


============================================================
TIL-046 — Prepa CCST : Module 2, composants reseau, types et connexions
============================================================
Date        : 2026-07-14
Projet      : Prepa CCST Networking
Catégorie   : Module 2

AVANCEMENT
100% - module termine.

ETAT
Module boucle. Je sais identifier les equipements et les supports d'un reseau et distinguer LAN et WAN.

NOTIONS VUES
- Peripheriques finaux (hotes) vs intermediaires (switch, routeur, point d'acces, pare-feu).
- Types de medias : cuivre (portee limitee), fibre (haut debit, longue distance), sans fil (mobilite).
- LAN (zone limitee, une organisation) vs WAN (relie des LAN eloignes).
- Options de connexion a Internet : DSL, cable, fibre, cellulaire, satellite.

A RETENIR
Les hotes echangent les donnees, les intermediaires les acheminent ; un WAN interconnecte plusieurs LAN.


============================================================
TIL-047 — Prepa CCST : Module 3, reseaux sans fil et mobiles (termine)
============================================================
Date        : 2026-07-14
Projet      : Prepa CCST Networking
Catégorie   : Module 3

AVANCEMENT
100% - module termine.

ETAT
Module boucle. Je sais configurer la connectivite sans fil d'un mobile : Wi-Fi, donnees cellulaires et Bluetooth.

NOTIONS VUES
- Types de reseaux sans fil selon la portee : WPAN (Bluetooth), WLAN (Wi-Fi), WMAN (ville), WWAN (cellulaire).
- Wi-Fi : normes IEEE 802.11 (a/b/g/n/ac/ax), bandes 2,4 GHz et 5 GHz, SSID protege en WPA2/WPA3.
- Wi-Fi vs donnees cellulaires (3G/4G/5G) : le mobile bascule selon la dispo et les reglages, le mode avion coupe tout.
- Bluetooth : WPAN courte portee, jumelage (pairing) puis connexion automatique ensuite.
- Configuration mobile : activer et choisir le SSID, gerer les donnees cellulaires (roaming, APN), jumeler en Bluetooth, partager la connexion (hotspot).

PROCHAINE ETAPE
Attaquer le module 4.


============================================================
TIL-048 — Migration de schema dans localStorage
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / localStorage

PROBLEME
Apres avoir change la structure des donnees (ancien tableau checks a 18 cases vers un objet ck.leaps/action/invest), les anciennes sauvegardes cassaient l'app.

CAUSE
localStorage garde l'ancien format tant que l'utilisateur ne vide pas son cache. Le code lisait une forme de donnees qui n'existait plus.

SOLUTION
A la lecture, detecter l'ancien champ et le convertir :
if(Array.isArray(p.checks) && p.checks.length===18 && !Array.isArray(store.ck.leaps)) store.ck.leaps=p.checks;
puis delete store.checks apres migration.

LECON
Des qu'on persiste des donnees cote client, il faut prevoir une migration de l'ancien schema vers le nouveau.


============================================================
TIL-049 — localStorage peut lever une exception
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / localStorage

PROBLEME
La sauvegarde plantait silencieusement dans certains contextes (fichier ouvert en file://, navigation privee, quota plein).

CAUSE
localStorage.setItem n'est pas garanti : il jette une exception si le stockage est indisponible ou sature, il ne se contente pas de retourner false.

SOLUTION
Encapsuler dans try/catch et prevenir l'utilisateur :
try{ localStorage.setItem(LS_KEY, JSON.stringify(store)) }
catch(e){ alert('Sauvegarde locale indisponible dans ce contexte.') }

LECON
Tout acces a localStorage doit etre protege, jamais suppose fiable.


============================================================
TIL-050 — Fusionner l'etat sauvegarde avec les valeurs par defaut
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : JavaScript / Etat

PROBLEME
Apres l'ajout d'un nouveau champ (watchlist), les anciennes sauvegardes le laissaient undefined et provoquaient des erreurs au rendu.

CAUSE
Un JSON.parse rend exactement ce qui a ete stocke : les nouveaux champs absents ne sont pas crees automatiquement.

SOLUTION
Partir d'un objet par defaut complet puis fusionner :
store = { ...store, ...JSON.parse(raw) };
et revalider chaque champ attendu (type, longueur de tableau) avant utilisation.

LECON
On fusionne toujours les donnees chargees par-dessus une structure par defaut, jamais l'inverse.


============================================================
TIL-051 — Enregistrer le service worker seulement en contexte securise
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / Service Worker

PROBLEME
Ouvrir l'app directement depuis le disque (file://) generait une erreur d'enregistrement du service worker.

CAUSE
L'API service worker n'existe qu'en contexte securise : https, localhost ou 127.0.0.1. Ailleurs navigator.serviceWorker est absent ou l'enregistrement echoue.

SOLUTION
Conditionner l'enregistrement :
if("serviceWorker" in navigator && (location.protocol==="https:" || location.hostname==="localhost" || location.hostname==="127.0.0.1")) navigator.serviceWorker.register("sw.js");

LECON
Un service worker ne fonctionne qu'en HTTPS ou en local ; il faut tester le contexte avant de l'enregistrer.


============================================================
TIL-052 — Cache du service worker fige apres une mise a jour
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / Service Worker

PROBLEME
Apres avoir modifie index.html, les utilisateurs continuaient de voir l'ancienne version de l'app.

CAUSE
Le service worker sert les fichiers depuis son cache. Tant que le nom du cache ne change pas, l'ancien contenu reste servi.

SOLUTION
Incrementer la constante de version (tradehub-v2, v3, ...) et supprimer les anciens caches a l'activation :
caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k))));

LECON
Chaque livraison doit changer la version du cache, sinon la mise a jour n'atteint jamais l'utilisateur.


============================================================
TIL-053 — Strategie network-first avec repli hors ligne
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / Service Worker

PROBLEME
Il fallait a la fois servir la derniere version quand le reseau est la et rester utilisable hors ligne.

CAUSE
Un cache-first sert du contenu perime ; un network-only casse hors ligne. Il faut arbitrer les deux.

SOLUTION
Dans le handler fetch : tenter le reseau, mettre a jour le cache avec la reponse (res.clone()), et sur echec retomber sur le cache, avec index.html comme repli pour la navigation :
.catch(() => caches.match(e.request).then(r => r || caches.match('./index.html')))

LECON
Network-first + repli cache donne du contenu frais en ligne et une app fonctionnelle hors ligne.


============================================================
TIL-054 — Chemins relatifs pour un deploiement GitHub Pages en sous-dossier
============================================================
Date        : 2026-07-14
Projet      : TradeHub
Catégorie   : PWA / Deploiement

PROBLEME
Sur GitHub Pages (URL de type user.github.io/tradehub/), des chemins absolus auraient pointe a la racine du domaine et casse le manifest, les icones et le service worker.

CAUSE
GitHub Pages sert le projet dans un sous-dossier, pas a la racine. Un chemin commencant par / ignore ce prefixe.

SOLUTION
Utiliser des chemins relatifs partout : start_url et scope a "./" dans le manifest, assets du cache en "./index.html", "./icon192.png", etc. Garder display "standalone" et des icones "any maskable" pour une vraie installation.

LECON
Une PWA hebergee dans un sous-chemin doit tout referencer en relatif, sinon rien ne charge en production.

============================================================
TIL-055 — Downgrade Spring Boot 4 vers 3.5 pour compatibilite ecosysteme
============================================================
Date        : 2026-07-21
Projet      : Scriptum
Categorie   : Backend / Spring Boot

PROBLEME
Spring Initializr avait genere le projet en Spring Boot 4.1.0 par defaut, sans que ce soit remarque. Springdoc-openapi (Swagger) plantait au demarrage avec une NoClassDefFoundError sur WebMvcProperties.

CAUSE
Boot 4 restructure des packages internes (Spring Framework 7, Hibernate 7, Jackson 3). Les librairies tierces comme springdoc 2.8.x ne sont pas encore compatibles avec cette nouvelle structure.

SOLUTION
Retour au parent spring-boot-starter-parent en 3.5.13. Remplacement de spring-boot-starter-webmvc par spring-boot-starter-web, et des artefacts de test specifiques par starter (data-jpa-test, security-test, etc., propres a Boot 4) par le seul spring-boot-starter-test existant en Boot 3.

LECON
Toujours verifier la version reelle du parent apres un scaffolding Spring Initializr, surtout sur un projet recent : la version par defaut proposee n'est pas toujours la plus stable pour l'ecosysteme de librairies tierces.

============================================================
TIL-056 — CORS bloque silencieusement les appels Angular vers Spring Boot
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / Securite

PROBLEME
Les appels HTTP depuis Angular (localhost:4200) vers le backend Spring Boot (localhost:8080) echouaient avec une erreur CORS sur le preflight OPTIONS, alors que les memes endpoints fonctionnaient parfaitement depuis Swagger UI.

CAUSE
Swagger UI est servi par le meme serveur (meme origine), donc jamais concerne par CORS. Spring Security, une fois une SecurityFilterChain personnalisee definie, n'ajoute aucune configuration CORS par defaut.

SOLUTION
Ajout explicite de .cors(cors -> cors.configurationSource(...)) dans la chaine de filtres, avec un bean CorsConfigurationSource autorisant l'origine http://localhost:4200, credentials inclus.

LECON
Un frontend et un backend sur des ports differents sont toujours consideres comme des origines differentes par le navigateur. Tester une API uniquement via Swagger ne suffit jamais a valider l'integration frontend.

============================================================
TIL-057 — sockjs-client incompatible avec un bundler moderne sans polyfill Node
============================================================
Date        : 2026-07-24
Projet      : Scriptum
Categorie   : Frontend / WebSocket

PROBLEME
ReferenceError: global is not defined au chargement de l'app Angular, provenant de sockjs-client.

CAUSE
sockjs-client a ete concu a l'origine pour Node.js et reference la variable globale global, absente nativement des navigateurs. Les imports ES modules etant hoistes, un polyfill ajoute en tete de fichier s'execute trop tard pour empecher le crash au chargement du module.

SOLUTION
Suppression complete de sockjs-client et .withSockJS() cote backend. Passage a un WebSocket natif via brokerURL (au lieu de webSocketFactory) dans la configuration du client STOMP, avec l'URL convertie en ws:// plutot que http://.

LECON
Face a une erreur de polyfill manquant sur une librairie tierce ancienne, verifier d'abord s'il existe une alternative moderne native avant de complexifier la configuration du bundler.

============================================================
TIL-058 — Fichiers de composants Angular vides sans erreur de compilation
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Frontend / Angular

PROBLEME
Le bouton d'upload et le panneau de resultat ne reagissaient a aucune interaction, sans la moindre erreur dans la console ni dans le terminal de build.

CAUSE
Les fichiers .component.ts et .component.html de plusieurs composants s'etaient retrouves vides suite a des copier-coller successifs entre composants generes au meme moment. Un template vide compile sans erreur : Angular affiche simplement un composant qui ne fait rien.

SOLUTION
Verification systematique du contenu reel des fichiers generes juste apres ng generate component, plutot que de supposer que le contenu colle a chaque etape s'est bien applique au bon fichier.

LECON
L'absence d'erreur de compilation ne garantit pas qu'un composant fonctionne. Un comportement silencieux (bouton qui ne fait rien, zone qui reste vide) doit toujours faire verifier le contenu reel des fichiers avant de chercher une cause plus complexe.

============================================================
TIL-059 — ffmpeg qui timeout a cause d'un deadlock sur ProcessBuilder
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / Process Java

PROBLEME
L'extraction audio via FfmpegService timeoutait systematiquement au bout de 5 minutes, alors que ffmpeg -version fonctionnait parfaitement en ligne de commande.

CAUSE
redirectErrorStream(true) fusionne stdout et stderr, mais le code ne lisait ce flux qu'apres waitFor(). ffmpeg ecrit un tres gros volume de logs de progression sur sa sortie. Le tampon systeme (pipe OS, environ 64 Ko) se remplit, ffmpeg se bloque en attente qu'on le lise, et Java attend de son cote la fin du processus avant de lire. Deadlock mutuel.

SOLUTION
Lancer un thread dedie qui consomme le flux de sortie en continu pendant l'execution du processus, avant meme d'appeler waitFor(), plutot que de lire seulement a la fin.

LECON
ProcessBuilder avec une sortie volumineuse impose toujours un lecteur de flux concurrent. Une commande courte (comme -version) peut fonctionner alors qu'une commande reelle bloque silencieusement pour cette raison.

============================================================
TIL-060 — Self-invocation Spring : @Async silencieusement ignore
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / Spring

PROBLEME
POST /api/jobs restait bloque plusieurs minutes avant de repondre, alors que processJob() etait bien annotee @Async.

CAUSE
startJob() appelait this.processJob(...) depuis l'interieur de la meme classe. Spring implemente @Async via un proxy genere autour du bean ; un appel interne (this.methode()) contourne completement ce proxy, l'execution redevient synchrone sans aucune erreur ni avertissement visible.

SOLUTION
Extraction de processJob() dans un service distinct (JobProcessingService), appele depuis JobOrchestratorService. L'appel devient externe entre deux beans, le proxy @Async s'applique enfin normalement.

LECON
@Async, @Transactional, @Cacheable et tous les mecanismes bases sur les proxies Spring AOP ne fonctionnent jamais en self-invocation. Reflexe a verifier systematiquement des qu'un comportement annote semble ignore : est-ce un appel this.methode() ou un appel vers un autre bean ?

============================================================
TIL-061 — @Transactional + @Async : race condition classique
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / Spring / Concurrence

PROBLEME
Apres correction du TIL-060, nouvelle erreur : IllegalStateException Job introuvable, levee dans le thread asynchrone alors que le job venait d'etre sauvegarde juste avant dans le meme appel.

CAUSE
startJob() etait annotee @Transactional. La transaction ne commit qu'a la toute fin de la methode. Le thread @Async demarre immediatement dans un thread separe et va chercher le job en base avant que la transaction principale ait fini de committer les donnees.

SOLUTION
Retrait de @Transactional sur startJob(). Un simple jobRepository.save() gere deja sa propre transaction courte, auto-committee immediatement, sans avoir besoin d'une transaction englobante ici.

LECON
Combiner @Transactional et @Async sur le meme chemin d'execution est une source classique de race condition. Une methode qui declenche un traitement asynchrone ne doit jamais retenir sa transaction plus longtemps que necessaire.

============================================================
TIL-062 — Depot Git imbrique detecte comme sous-module fantome
============================================================
Date        : 2026-07-21
Projet      : Scriptum
Categorie   : Git

PROBLEME
Sur GitHub, le dossier scriptum-api apparaissait avec une fleche au lieu de s'ouvrir normalement, comme un sous-module classique.

CAUSE
scriptum-api possedait son propre dossier .git interne, probablement cree automatiquement par IntelliJ lors du scaffolding initial du module, sans que ce soit remarque. Git parent traite alors ce dossier comme un depot imbrique (sous-module) plutot que comme des fichiers normaux.

SOLUTION
Suppression du .git imbrique (Remove-Item -Recurse -Force scriptum-api\.git), retrait de la reference de sous-module (git rm --cached scriptum-api), puis reintegration normale (git add scriptum-api).

LECON
Apres avoir genere un projet dans un sous-dossier d'un monorepo, toujours verifier la presence d'un .git imbrique avant le tout premier commit. L'IDE peut initialiser Git sans qu'on le demande explicitement.

============================================================
TIL-063 — Fuite de cle API via .idea/workspace.xml
============================================================
Date        : 2026-07-21
Projet      : Scriptum
Categorie   : Git / Securite

PROBLEME
git push rejete par GitHub Push Protection : une cle API Groq detectee dans l'historique des commits.

CAUSE
La cle avait ete saisie dans une configuration d'execution IntelliJ (variables d'environnement du Run Configuration), stockee en clair dans .idea/workspace.xml. Ce fichier n'etait pas dans le .gitignore des le depart, donc Git l'a suivi et commit avec la cle dedans.

SOLUTION
Revocation immediate de la cle fuitee (jamais la reutiliser meme apres nettoyage). Ajout de .idea/ au .gitignore. Reconstruction complete de l'historique Git (.git supprime puis reinitialise), la cle etant deja presente dans plusieurs commits anterieurs, un simple amend ne suffisait pas.

LECON
.idea/ doit etre dans le .gitignore avant le tout premier commit, jamais ajoute apres coup. Les secrets places dans les Run Configurations IntelliJ ne sont pas plus surs que du hardcoding si ce dossier de config est suivi par Git. Un fichier .env centralise, independant de l'IDE, reste plus sur.

============================================================
TIL-064 — MyMemory renvoie une traduction null sans erreur HTTP
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / API externe

PROBLEME
L'appel de traduction repondait 200 OK, mais translatedText valait null, affiche cote frontend comme le texte litteral "null" concatene au reste.

CAUSE
Whisper (via Groq) renvoie la langue detectee comme un nom complet ("french"), pas un code ISO ("fr"). MyMemory attend un vrai code ISO 639-1 dans son parametre langpair ; avec "french|en", l'API ne reconnait pas la langue et renvoie silencieusement un responseData.translatedText a null, sans lever d'erreur HTTP.

SOLUTION
Ajout d'une table de conversion nom Whisper vers code ISO avant l'appel MyMemory. Retour du texte original (au lieu de null) si la reponse est invalide, pour eviter la concatenation d'un "null" litteral dans le resultat final.

LECON
Un code 200 OK ne garantit pas un contenu exploitable. Toujours logger la reponse brute d'une API tierce avant de suspecter un bug de parsing cote client ; ici le vrai probleme etait une incompatibilite de format de donnees entre deux services, pas une erreur de code.

============================================================
TIL-065 — Groq ne renvoie la langue detectee qu'en format verbose
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / API externe

PROBLEME
sourceLanguage restait toujours null en base de donnees, alors que la transcription elle-meme fonctionnait parfaitement.

CAUSE
L'API Groq ne renvoie le champ language que si response_format=verbose_json est explicitement demande dans la requete multipart. Par defaut, elle ne renvoie que le champ text.

SOLUTION
Ajout de builder.part("response_format", "verbose_json") dans l'appel WebClient vers Groq.

LECON
Ne jamais supposer qu'une API renvoie toutes les metadonnees disponibles par defaut. Toujours verifier la documentation pour les champs optionnels (langue, timestamps, score de confiance) qui necessitent souvent un parametre explicite en plus du resultat principal attendu.

============================================================
TIL-066 — Mot de passe genere par Spring Security malgre une SecurityConfig personnalisee
============================================================
Date        : 2026-07-22
Projet      : Scriptum
Categorie   : Backend / Spring Security

PROBLEME
Le message Using generated security password apparaissait au demarrage et un login/mot de passe etaient exiges sur tous les endpoints, alors qu'une SecurityFilterChain personnalisee etait deja definie.

CAUSE
La generation du mot de passe par defaut (UserDetailsServiceAutoConfiguration) est un mecanisme independant de la SecurityFilterChain. Elle se declenche des qu'aucun bean UserDetailsService n'est present dans le contexte, peu importe la config de filtres deja ecrite.

SOLUTION
Exclusion explicite de UserDetailsServiceAutoConfiguration dans @SpringBootApplication en attendant l'implementation du vrai systeme d'authentification JWT (User, UserDetailsServiceImpl), qui a ensuite pris le relai naturellement.

LECON
Une SecurityFilterChain personnalisee ne suffit pas a elle seule a desactiver tous les comportements par defaut de Spring Security. Certaines auto-configurations (comme la generation d'utilisateur par defaut) doivent etre exclues explicitement tant qu'aucun UserDetailsService reel n'existe.

============================================================
TIL-067 — Swagger n'affiche pas le cadenas d'authentification sans schema declare
============================================================
Date        : 2026-07-24
Projet      : Scriptum
Categorie   : Backend / Documentation API

PROBLEME
Impossible de tester les endpoints proteges par JWT depuis Swagger UI : aucun bouton Authorize (cadenas) n'apparaissait dans l'interface.

CAUSE
Springdoc ne fait apparaitre le bouton Authorize que si un schema de securite est explicitement declare dans la configuration OpenAPI. Une SecurityConfig Spring valide ne suffit pas, Swagger n'a aucune connaissance automatique du mecanisme d'authentification utilise par l'API.

SOLUTION
Ajout d'un bean SecurityScheme de type HTTP bearer avec bearerFormat JWT dans OpenApiConfig, associe via addSecurityItem. Le bouton Authorize est alors apparu, acceptant un token colle sans le prefixe Bearer (ajoute automatiquement par Swagger une fois le schema declare).

LECON
La documentation OpenAPI d'une API securisee doit declarer explicitement son schema d'authentification ; sans ca, Swagger UI reste utilisable uniquement pour les routes publiques, sans moyen visible de s'authentifier pour les autres.

============================================================
TIL-068 — Quota gratuit MyMemory epuise en pleine session de tests
============================================================
Date        : 2026-07-25
Projet      : Scriptum
Categorie   : Backend / API externe

PROBLEME
Un job de transcription complet echouait en FAILED a l'etape de traduction, avec une erreur 429 Too Many Requests provenant de l'API MyMemory, alors que la transcription elle-meme avait parfaitement reussi.

CAUSE
Le plan gratuit de MyMemory est limite a 5000 caracteres par jour sans email associe a la requete, une limite tres vite atteinte apres plusieurs tests successifs de traduction dans la meme journee. Le code ne geerait pas ce cas et laissait l'exception remonter, faisant echouer tout le job pour un probleme qui ne concernait que l'etape optionnelle de traduction.

SOLUTION
Encapsulation de l'appel MyMemory dans un bloc try/catch qui retourne le texte original en cas d'echec (quota, erreur reseau, reponse invalide), au lieu de laisser planter tout le pipeline pour une fonctionnalite annexe.

LECON
Un service externe optionnel (ici la traduction) ne doit jamais pouvoir faire echouer une fonctionnalite principale (ici la transcription) deja aboutie. Prevoir un degrade gracieux plutot qu'un echec total des qu'une dependance secondaire est indisponible ou limitee.


============================================================
MODELE TIL A COPIER
============================================================

------------------------------------------------------------
TIL-XXX — Titre clair du problème
------------------------------------------------------------
Date        :
Projet      :
Catégorie   :

PROBLEME

CAUSE

SOLUTION

LECON
------------------------------------------------------------


============================================================
MODELE TIL A COPIER
============================================================
...
============================================================
MODELE ETAT PREPA A COPIER
============================================================

------------------------------------------------------------
TIL-XXX — Prepa  : Module N, titre du module (en cours / termine)
------------------------------------------------------------
Date        :
Projet      : Prepa 
Catégorie   : Module N

AVANCEMENT
(ex : 45% - sections vues, ce qu'il reste / 100% - module termine)

ETAT
(ce que je fais actuellement, ou ce que je maitrise si termine)

NOTIONS VUES
-
-
-

PROCHAINE ETAPE
(la suite : finir une section, recapitulatif, module suivant)
------------------------------------------------------------